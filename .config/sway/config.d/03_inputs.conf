# ==============================================================================
# Input Configuration (Keyboard, Touchpad, Pointer)
# ==============================================================================

input type:touchpad {
    dwt enabled
    tap enabled
    natural_scroll enabled
    middle_emulation enabled
    accel_profile adaptive
    pointer_accel 0.2
}

input type:keyboard {
    # US International with AltGr Dead Keys
    xkb_layout "us"
    xkb_variant "altgr-intl"
    repeat_delay 250
    repeat_rate 35
}
